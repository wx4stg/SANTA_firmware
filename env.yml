name: slowant
channels:
  - conda-forge
dependencies:
  - numpy
  - pip
  - pyserial
  - python
  - pip:
    - RPi.GPIO
    - lgpio # warning, need to clone and compile https://github.com/joan2937/lg before installing!
    - adafruit-circuitpython-bme280
